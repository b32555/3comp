#include <iostream>
#include <cmath>

int main() {
    double sum = 0.0;

    for (int k = 1; k <= 24; ++k) {
        sum += pow(-1, k-1) / (2*k + 1);
    }

    std::cout << "The sum of the series is: " << sum << std::endl;
    return 0;
}
